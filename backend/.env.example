GEMINI_API_KEY=AIza...

# Supabase (proyecto: myunwdrginkskmxmsjgv) — usado por app/core/auth.py para validar
# el JWT del usuario vía JWKS, y disponible para módulos futuros que necesiten la
# secret key (acceso admin/service-role).
SUPABASE_URL=https://myunwdrginkskmxmsjgv.supabase.co
SUPABASE_SECRET_KEY=sb_secret_...

# Fase 5.1 — allowlist de acceso (defensa en profundidad, además del auto-registro
# deshabilitado en el panel de Supabase). Comparación por sufijo, case-insensitive.
# Si ninguna de las dos está seteada, no se bloquea a nadie (solo se loguea un warning).
ALLOWED_EMAIL_DOMAINS=tailwindgc.com
# ALLOWED_EMAILS=alguien@externo.com,otra@externo.com
